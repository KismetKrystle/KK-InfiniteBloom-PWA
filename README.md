
  # PWA Landing Page for HTML Flipbook

  This is a code bundle for PWA Landing Page for HTML Flipbook. The original project is available at https://www.figma.com/design/QDlpKLSkreV6v6hIFIbp1O/PWA-Landing-Page-for-HTML-Flipbook.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  